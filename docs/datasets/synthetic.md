# Synthetic

The cache uses a deterministic LEAF-style generator with 30 clients, 10
classes, and 60 features.  Alpha, beta, generator seed, splits, IDs, and
generator identifier are recorded in the manifest.  It applies no feature
normalization.
